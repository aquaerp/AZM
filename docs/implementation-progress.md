# متابعة تنفيذ خطة إكمال عزم

آخر تحديث: 2026-08-27

| المرحلة | الحالة | الدليل الحالي | المتبقي للعبور |
|---|---|---|---|
| 1. المصدر والإصدارات | مكتملة | نسخة موحدة 1.2.1، Changelog، سياسة إصدار، Dependabot، ربط `aquaerp/AZM`، نجاح Quality Gate على PR و`main`، ونشر الالتزام `047bfbf` على الإنتاج. أُنشئ فرع `develop` وفُعّلت قواعد `Protect main and develop` على الفرعين: تمنع الحذف وforce push، وتُلزم بطلب دمج وحل المحادثات وتحديث الفرع ونجاح فحوص Quality Gate الستة | — |
| 2. الجودة وE2E | مكتملة | 13 سيناريو Playwright و67 اختبار Django ناجحة. نجحت رحلة UAT متكاملة على Staging المعزول (`2a95fca7`) وشملت تصحيح الفاتورة، تنزيل PDF محميًا، التسليم، المخزون والعمولة. اعتمد مالك المشروع نموذج UAT إلكترونيًا بتاريخ 2026-08-21 | — |
| 6. التشغيل والتعافي | مكتملة | نجح اختبار الاستعادة المعزول وتطابقت عدادات قاعدة البيانات و67/67 ملف وسائط بزمن عودة أقل من 30 ثانية بعد توفر النسخة محليًا. يعمل النسخ الخارجي إلى S3 بمستخدم IAM وسياسة محدودين بالمسار `azm/*`؛ رُفعت مجموعتان كاملتان واجتاز كل ملف `rclone check`، وعمل heartbeat دون تحذير، وأصبح مؤقت `systemd` اليومي مفعّلًا. الدليل في `docs/recovery-drill-2026-08-21.md` | — |
| 3. إعادة هيكلة الواجهة | مكتملة | فُصل شريط التنقل والترويسة وصفحات لوحة الملخص وبطاقات العمل والسجلات العامة والمهام والموظفين والعمولات والمخزون والمحاسبة ونقطة البيع والوثائق وإعدادات الورشة عن `Workspace.jsx`. عُمم Pagination على القوائم الطويلة، بما فيها فواتير نقطة البيع والوثائق وتنبيهاتها. انخفض الملف من نحو 79.7 إلى 30.6 كيلوبايت، ونجحت lint واختبارات الواجهة (20/20) والبناء و13/13 سيناريو Playwright بتاريخ 2026-08-22 | — |
| 4. الأداء | قيد التنفيذ | فُعّل pool PostgreSQL ورُقمت قوائم API الأساسية اختياريًا. نجح اختبار 100 ألف سجل بواقع 300,007 طلبات دون فشل، وP95 قدره 755.86ms ومعدل 63.26 طلب/ثانية؛ تطابقت عدادات العملاء والمركبات والبطاقات عند 100,000 لكل نوع، وبقي أبطأ مسار P95 عند 817.96ms. بلغ حجم القاعدة 231MB وذاكرة PostgreSQL 151MiB بعد الاختبار | نقل الواجهة تدريجيًا إلى ترقيم API حتى تستفيد الشاشات نفسها من الأداء المثبت على الخوادم الكبيرة |
| 5. المحاسبة والامتثال | لم تبدأ | تعديل المسودة وPDF وQR موجود | إشعارات دائنة/مدينة وعكس الدفعات وZATCA المرحلة الثانية |
| 7. Pilot والإطلاق | لم تبدأ | — | UAT ميداني وقياسات و14 يوم استقرار |

لا تعتبر المرحلة مغلقة لمجرد وجود الكود؛ يجب تحقيق معيار القبول الموثق وإرفاق نتيجة اختبار أو تشغيل فعلية.
