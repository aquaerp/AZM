# تصميم المعمارية التقنية وهيكلية البيانات الكلية لنظام إدارة ورش السيارات

يهدف هذا المستند إلى توفير نظرة شاملة للمعمارية التقنية وهيكلية البيانات لنظام إدارة ورش السيارات، مع التركيز على المتطلبات التجارية لضمان قابلية التوسع والأمان والأداء.

## 1. المعمارية العامة للنظام

يعتمد النظام على معمارية **Modular Monolith** باستخدام **Python/Django** للواجهة الخلفية (Backend) و **React.js** للواجهة الأمامية (Frontend). هذا النهج يجمع بين سهولة التطوير والصيانة للأنظمة المتجانسة مع إمكانية فصل الوحدات مستقبلاً عند الحاجة. يتم التواصل بين الواجهتين عبر واجهات برمجية (APIs) من نوع RESTful.

### المكونات الرئيسية:

*   **الواجهة الأمامية (Frontend)**: تطبيق ويب مبني باستخدام React.js و Tailwind CSS، يوفر واجهة مستخدم تفاعلية ومتجاوبة لمختلف الأدوار (مدير الورشة، المحاسب، الفني).
*   **الواجهة الخلفية (Backend)**: تطبيق Django يوفر منطق الأعمال (Business Logic)، إدارة قواعد البيانات، واجهات برمجة التطبيقات (APIs)، وإدارة المهام الخلفية.
*   **قاعدة البيانات (Database)**: PostgreSQL، لضمان سلامة البيانات، دعم المعاملات المعقدة، وقابلية التوسع.
*   **نظام المهام المجدولة (Task Queue)**: Celery مع Redis كـ Broker، لمعالجة المهام الخلفية غير المتزامنة مثل إرسال التنبيهات ومعالجة الوثائق.
*   **نظام التخزين السحابي (Cloud Storage)**: AWS S3 (أو حل متوافق مثل MinIO)، لتخزين الوثائق والملفات الكبيرة بشكل آمن ومشفر.

## 2. هيكلية قاعدة البيانات (PostgreSQL Schema)

تم تصميم قاعدة البيانات لتكون مركزاً لجميع العمليات، مع علاقات واضحة بين الكيانات لضمان تكامل البيانات.

### الجداول الأساسية:

| الجدول | الوصف | العلاقات الرئيسية |
| :--- | :--- | :--- |
| `Users` | معلومات المستخدمين، أدوارهم (مدير، فني، محاسب)، وبيانات الاعتماد. | `One-to-Many` مع `Employees` |
| `Workshops` | تفاصيل الورشة (الاسم، العنوان، معلومات الاتصال). | `One-to-Many` مع `Users`, `Employees` |
| `Customers` | بيانات العملاء (الاسم، الهاتف، البريد الإلكتروني). | `One-to-Many` مع `Vehicles`, `JobCards` |
| `Vehicles` | تفاصيل المركبات (النوع، الموديل، رقم الهيكل، لوحة الترخيص). | `Many-to-One` مع `Customers`, `One-to-Many` مع `JobCards` |
| `JobCards` | قلب النظام: تفاصيل أمر الإصلاح (التاريخ، الحالة، الوصف، التكلفة التقديرية والفعلية). | `Many-to-One` مع `Customers`, `Vehicles`, `Employees` (الفني المسؤول) |
| `Services` | قائمة بالخدمات المقدمة (الاسم، الوصف، السعر). | `Many-to-Many` مع `JobCards` |
| `Parts` | إدارة المخزون (الاسم، رقم المنتج، المورد، الكمية، سعر الشراء، سعر البيع، حد إعادة الطلب). | `Many-to-Many` مع `JobCards`, `Many-to-One` مع `Suppliers` |
| `Suppliers` | معلومات الموردين لقطع الغيار. | `One-to-Many` مع `Parts` |
| `Employees` | بيانات الموظفين (الاسم، الوظيفة، تاريخ التوظيف، نسبة العمولة). | `Many-to-One` مع `Users` |
| `Invoices` | الفواتير الصادرة (الرقم، التاريخ، المبلغ الإجمالي، الضرائب، حالة الدفع). | `One-to-One` مع `JobCards` |
| `Expenses` | تسجيل المصروفات التشغيلية للورشة. | لا توجد علاقات مباشرة مع الكيانات الرئيسية |
| `Documents` | أرشفة الوثائق (الاسم، النوع، مسار الملف في S3، تاريخ الانتهاء، الكيان المرتبط). | `Many-to-One` مع `Customers`, `Vehicles`, `Employees` |
| `Notifications` | سجل التنبيهات المرسلة (النوع، الرسالة، التاريخ، حالة الإرسال). | `Many-to-One` مع `Users` |

## 3. واجهات برمجة التطبيقات (API Endpoints)

سيتم بناء واجهات برمجة تطبيقات RESTful باستخدام Django REST Framework لتسهيل التواصل بين الواجهة الأمامية والخلفية، وتوفير نقاط وصول آمنة للبيانات.

### أمثلة على Endpoints:

*   `/api/auth/login`, `/api/auth/register`
*   `/api/jobcards/`, `/api/jobcards/<id>/`
*   `/api/parts/`, `/api/parts/<id>/`, `/api/parts/low-stock/`
*   `/api/employees/`, `/api/employees/<id>/`
*   `/api/invoices/`, `/api/invoices/<id>/generate-pdf/`
*   `/api/documents/`, `/api/documents/<id>/upload/`
*   `/api/notifications/`

## 4. آليات الأمان والتشفير

*   **المصادقة والتفويض (Authentication & Authorization)**: استخدام JSON Web Tokens (JWT) للمصادقة، ونظام التحكم في الوصول المستند إلى الأدوار (RBAC) لتحديد صلاحيات المستخدمين.
*   **تشفير البيانات**: تشفير البيانات الحساسة في قاعدة البيانات (Data at Rest Encryption) وتشفير الاتصال (Data in Transit Encryption) باستخدام HTTPS.
*   **تشفير الوثائق**: تشفير الملفات المخزنة في S3 باستخدام AES-256 قبل رفعها، مع إدارة مفاتيح التشفير بشكل آمن.

## 5. نظام التنبيهات والمهام الخلفية

سيتم استخدام Celery لمعالجة المهام التي تتطلب وقتاً طويلاً أو المهام المجدولة، مثل:

*   **تنبيهات انتهاء الوثائق**: فحص يومي لتواريخ انتهاء صلاحية الوثائق وإرسال تنبيهات قبل 45، 30، و 7 أيام عبر البريد الإلكتروني أو إشعارات داخل التطبيق.
*   **تنبيهات المخزون**: إرسال تنبيهات عند وصول كمية قطع الغيار إلى حد إعادة الطلب.
*   **توليد التقارير**: إنشاء تقارير مالية أو تشغيلية بشكل دوري.

## 6. التكاملات الخارجية المقترحة

*   **بوابة دفع (Payment Gateway)**: لتسهيل عمليات الدفع الإلكتروني للفواتير.
*   **خدمة رسائل (SMS/WhatsApp API)**: لإرسال إشعارات للعملاء بحالة سياراتهم أو تنبيهات للموظفين.
*   **نظام إدارة علاقات العملاء (CRM)**: لتعزيز التواصل مع العملاء وتقديم خدمة أفضل.

هذه المعمارية توفر أساساً متيناً وقابلاً للتوسع لتلبية جميع متطلبات نظام إدارة ورش السيارات، مع التركيز على الأداء والأمان وسهولة الصيانة.
